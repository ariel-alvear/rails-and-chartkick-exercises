<h1>Dashboard#index</h1>
<p>Find me in app/views/dashboard/index.html.erb</p>

<%= column_chart Sale.group("strftime('%m-%Y', date_time)").order(date_time: :asc).sum(:amount) %>

<%= pie_chart Sale.group(:variety).count %>
